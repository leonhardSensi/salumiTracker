extends base

block content
  .container(style="background: #fff7ef; padding: 2rem; border-radius: 12px; font-family: Georgia, serif; color: #5a3e36; max-width: 600px; margin: auto;")

    h1(style="font-size: 24px; text-align: center; color: #b2613c; margin-bottom: 1rem;") Your Salumi Are Nearly Ready!
    p(style="font-size: 16px; margin-bottom: 1rem;") Hi #{firstName},

    p(style="font-size: 16px; margin-bottom: 1rem;") Here's a quick heads-up: the following salumi are nearing completion or already ready to harvest:

    ul(style="padding-left: 1.2rem; margin-bottom: 1.5rem;")
      -
        const list = salumiToNotifyArr
      each item in list 
        if (item.daysRemaining < 0) 
          li(style="margin-bottom: 0.5rem;") 
            strong #{item.salume} 
            |  – ready 
            span(style="color: #a33; font-weight: bold") #{item.daysRemaining.toString().replace('-', '')} day(s) ago!
        else 
          li(style="margin-bottom: 0.5rem;") 
            strong #{item.salume} 
            |  – 
            span(style="font-weight: bold") #{item.daysRemaining} day(s) left

    // Button
    table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0' style="margin: 2rem auto;")
      tbody
        tr
          td(align='center')
            a(href='http://localhost:3000/dashboard' target='_blank' style='background-color: #b2613c; color: white; text-decoration: none; padding: 12px 24px; border-radius: 8px; font-size: 16px; display: inline-block;')
              | Check Your Salumi Now

    p(style="font-size: 14px; margin-top: 2rem; text-align: center; color: #8a6c5c;")
      | Keep an eye on your batches and harvest at the perfect time.
      br
      | Buon lavoro!
      br
      em – Your Salumi Tracker Team
