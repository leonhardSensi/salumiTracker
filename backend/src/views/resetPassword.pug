//- extends base

//- block content
//-   p Hi #{firstName},
//-   p Forgot password? Send a PATCH request to with your password and passwordConfirm to #{url}
//-   table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0')
//-     tbody
//-       tr
//-         td(align='left')
//-           table(role='presentation' border='0' cellpadding='0' cellspacing='0')
//-             tbody
//-               tr
//-                 td
//-                   a(href=`${url}` target='_blank') Reset password
//-   p If you didn't forget your password, please ignore this email
//-   p Good luck! Your Salumi Tracker Team

doctype html
html
  head
    meta(charset="UTF-8")
    title Reset Your Password - Salumi Tracker
    style.
      body {
        background-color: #f9f5f0;
        font-family: 'Georgia', serif;
        color: #5a3e36;
        padding: 2rem;
      }
      .container {
        background: #fff7ef;
        border: 1px solid #e0d4c0;
        border-radius: 12px;
        padding: 2rem;
        max-width: 600px;
        margin: auto;
        box-shadow: 0 4px 8px rgba(0,0,0,0.05);
      }
      h1 {
        text-align: center;
        font-size: 28px;
        margin-bottom: 1rem;
      }
      p {
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 1rem;
      }
      .button {
        display: block;
        width: fit-content;
        background-color: #b2613c;
        color: white;
        text-decoration: none;
        padding: 0.75rem 1.5rem;
        border-radius: 8px;
        font-size: 16px;
        margin: 2rem auto 1rem;
        text-align: center;
      }
      .footer {
        text-align: center;
        font-size: 14px;
        color: #a48d7c;
        margin-top: 2rem;
      }

  body
    .container
      h1 Reset Your Password
      p Hello #{firstName},
      p We received a request to reset your password for your Salumi Tracker account.
      p If you made this request, click the button below to create a new password.
      a.button(href=`${url}`) Reset My Password
      p If the button doesn’t work, copy and paste this link into your browser:
      p #{url}
      p If you didn’t request a password reset, you can safely ignore this email.
      .footer
        | Salumi Tracker · Made with 🍷 & 🧂
